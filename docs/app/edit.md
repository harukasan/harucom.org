---
layout: doc
title: テキストエディタ
permalink: /docs/app/edit/
lang: ja
ref: docs-app-edit
---

`edit` は Harucom に入っている全画面のテキストエディタです。
Ruby のスクリプトを書いたり、設定ファイルを直したりするのに使います。

## 目次

- [起動する](#起動する)
- [画面の見かた](#画面の見かた)
- [キー操作](#キー操作)
- [保存する](#保存する)
- [終了する](#終了する)
- [シンタックスハイライトと自動インデント](#シンタックスハイライトと自動インデント)

## 起動する

IRB のプロンプトで `edit` に続けて編集するファイル名を入力します。

```ruby
irb> edit hello.rb
```

指定されたファイルがなければ新しく作ります。
ある場合はその内容を開きます。

ファイル名を付けずに起動することもできます。
その場合、ファイルを保存するときに名前を指定できます。

```ruby
irb> edit
```

`cd` でディレクトリを移動しているときは、ファイル名だけでそのディレクトリのファイルを開けます。
`/` からパスを指定することもできます。

```ruby
irb> edit /app/hello.rb
```



## 画面の見かた

いちばん上にあるステータスバーにはファイル名とカーソル位置を表示します。

```
 hello.rb [+]  12:5
```

`[+]` は、まだ保存していない変更があることを示しています。
ファイル名をつけていない場合は `[untitled]` と表示されます。

## キー操作

| キー | 動作 |
|------|------|
| <kbd>→</kbd> / <kbd>↑</kbd> / <kbd>←</kbd> / <kbd>↓</kbd> | カーソルを移動する |
| <kbd>Home</kbd> / <kbd>End</kbd> | 行頭に移動する / 行末に移動する |
| <kbd>PageUp</kbd> / <kbd>PageDown</kbd> | ページスクロール |
| <kbd>Backspace</kbd> | カーソルの前の文字を消す |
| <kbd>Delete</kbd> | カーソルの位置の文字を消す |
| <kbd>Enter</kbd> | 改行する |
| <kbd><kbd>Ctrl</kbd>-<kbd>S</kbd></kbd> | 保存する |
| <kbd><kbd>Ctrl</kbd>-<kbd>Q</kbd></kbd> | 終了する |
| <kbd><kbd>Ctrl</kbd>-<kbd>Z</kbd></kbd> | 元に戻す |
| <kbd><kbd>Ctrl</kbd>-<kbd>Y</kbd></kbd> | やり直す |

## 保存する

<kbd><kbd>Ctrl</kbd>-<kbd>S</kbd></kbd> で保存します。

ファイル名がまだ決まってない場合は保存するときに名前を聞かれます。
<kbd>Esc</kbd> を押すと保存をキャンセルできます。

## 終了する

<kbd><kbd>Ctrl</kbd>-<kbd>Q</kbd></kbd> で終了して IRB に戻ります。

保存していない変更があるときは確認されます。

```
 Unsaved changes. Quit? (y/n):
```

<kbd>y</kbd> を押すと保存せずに終了し、<kbd>n</kbd> を押すと編集に戻ります。

## シンタックスハイライトと自動インデント

ファイル名が `.rb` で終わるときは、Ruby のコードを書きやすくするしくみが動きます。

### シンタックスハイライト

文字列やキーワード、コメント、数値などが色分けして表示されます。
書きかけの `end` や閉じていない文字列にも気づきやすいです。

### 自動インデント

`def` や `if`、`do` のあとで改行すると、字下げ(インデント)が自動で入ります。
`end` や `else` を打つと、その行の字下げが勝手に正しい位置に戻ります。

```ruby
def greet(name)
  puts "Hello, #{name}!"
end
```

## 関連ページ

- [日本語を入力する](../../japanese-input/) - テキストエディタでも日本語を入力できます
- [コマンド](../../commands/) — `cat` や `cp` などのファイル操作
- [ファイルの入出力](../../files/) — ファイルの置き場所とスクリプトの実行
- [基本的な使い方](../../getting-started/) — はじめてのプログラムを書く
